spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          #basedir:
          uri:
          search-paths:
          default-label:
          username:
          password:
server:
  port: 8888